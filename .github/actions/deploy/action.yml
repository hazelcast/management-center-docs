name: Deploy docs
description: Trigger Netlify production deployment

runs:
  using: 'composite'
  steps:
    - name: Trigger docs deployment
      run: curl -X POST -d {} https://api.netlify.com/build_hooks/6001915875dde5f2ff105296
