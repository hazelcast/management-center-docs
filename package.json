{
  "name": "hazelcast-docs-playbook",
  "version": "1.0.0",
  "description": "Hazelcast Antora project.",
  "author": "Hazelcast Docs",
  "license": "ISC",
  "scripts": {
    "load-antora-playbook": "node global-playbook-loader.js",
    "build": "npm run load-antora-playbook && antora --to-dir docs --fetch global-antora-playbook.yml",
    "load-antora-playbook-local": "node global-playbook-loader.js -b main -r hazelcast/management-center-docs --skip-private-repos",
    "build-local": "npm run load-antora-playbook-local && antora --to-dir test --fetch antora-playbook-local.yml",
    "check-orphan-pages": "check-orphan-pages --log-failure-level=error",
    "serve": "serve test",
    "expose": "ngrok http 5000"
  },
  "devDependencies": {
    "@antora/cli": "^3.1.1",
    "@antora/site-generator": "^3.1.1",
    "asciidoctor-kroki": "^0.10.0",
    "hazelcast-docs-tools": "github:hazelcast/hazelcast-docs-tools#v1.1.0",
    "matcher": "^3.0.0",
    "ngrok": "^4.2.2",
    "serve": "^13.0.2",
    "yaml": "^2.6.0"
  }
}
